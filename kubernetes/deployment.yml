apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus
  labels:
    repo: gcp-oidc-token-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      repo: gcp-oidc-token-proxy
  template:
    metadata:
      labels:
        repo: gcp-oidc-token-proxy
    spec:
      containers:
        - name: prometheus
          image: docker.io/prom/prometheus:v2.30.2
          imagePullPolicy: Always
          args:
          - --config.file=/etc/prometheus/prometheus.yml
          - --web.enable-lifecycle
          volumeMounts:
          - name: config
            mountPath: /etc/prometheus
            readOnly: true
        - name: gcp-oidc-token-proxy
          image: ghcr.io/dazwilkin/gcp-oidc-token-proxy:bed00a2ddd7a1fc15031e790881329ce21bc591e
          imagePullPolicy: Always
          env:
          - name: GOOGLE_APPLICATION_CREDENTIALS
            value: /secrets/key.json
          args:
            - --port=7777
          volumeMounts:
            - name: secrets
              mountPath: /secrets
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: CONFIG
        - name: secrets
          secret:
            secretName: SECRET
